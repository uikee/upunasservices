server:
  port: 8093
spring:
  application:
    name: api-gateway
  zipkin:
    enabled: true
  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka
      fetch-registry: true
      register-with-eureka: true
